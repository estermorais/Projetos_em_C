#include <stdio.h>
#include <stdlib.h>

int primo (int num){
	
	if(num==2)
	return 1;

	if(num!=2, num%2 == 0)
	return 0;

	if(num%div==0)
	return 0;

	else
	return 1;

	}
	

int main (){

	int num = 0;
	
	scanf("%d", %num);
	printf("%d", primo(num));
}